# Sinbad Portal - Система Локальных Бэкапов v2.0

## Обзор

Автоматическая система резервного копирования для безопасной работы с проектом Sinbad Portal. Бэкапы создаются автоматически при **любых** Git-событиях с проверкой реальных изменений.

## Что нового в v2.0

✨ **Новые возможности:**
- Бэкапы при **всех** Git-операциях (commit, merge, pull, checkout, rebase, amend)
- Проверка изменений - нет дубликатов при отсутствии изменений
- Опциональное включение базы данных (`--include-db`)
- Baseline бэкапы с полным снимком проекта
- Включение каталога `forum/` в бэкапы
- Улучшенное логирование с причиной создания бэкапа

## Структура

```
~/SinbadBackups/
├── 2025-12-05_19-35-35/    # BASELINE бэкап
│   ├── app/
│   ├── forum/              # ← Теперь включён
│   ├── db_dump/            # ← Опционально
│   │   └── *.sql
│   ├── .backup-info        # Метаданные
│   └── .backup-manifest    # Список файлов
├── 2025-12-05_20-00-00/    # Обычный бэкап
├── backup.log              # Лог всех операций
└── ...
```

## Автоматическое создание бэкапов

Бэкапы создаются автоматически при:

### 1. Git Commit
```bash
git commit -m "Update feature"
# → автоматически создаётся бэкап (причина: commit)
```

### 2. Git Merge/Pull
```bash
git pull origin main
# → автоматически создаётся бэкап (причина: merge)
```

### 3. Git Checkout
```bash
git checkout feature-branch
# → автоматически создаётся бэкап (причина: checkout)
```

### 4. Git Rebase/Amend
```bash
git commit --amend
git rebase main
# → автоматически создаётся бэкап (причина: rewrite-amend/rewrite-rebase)
```

### 5. Git Apply Patch
```bash
git am patch.diff
# → автоматически создаётся бэкап (причина: applypatch)
```

### 6. Вручную
```bash
# Обычный бэкап
.git/hooks/backup-manager.sh create

# С базой данных
.git/hooks/backup-manager.sh create --include-db

# Baseline бэкап (полный снимок)
.git/hooks/backup-manager.sh create --baseline
```

## Проверка изменений

**Важно:** Бэкап создаётся только при наличии реальных изменений!

Система проверяет:
- Незакоммиченные изменения
- Отличие от последнего бэкапа по Git commit hash
- Если изменений нет → бэкап пропускается

Чтобы создать бэкап принудительно:
```bash
.git/hooks/backup-manager.sh create --force
```

## Типы бэкапов

### Baseline бэкап
Полный снимок проекта, включает:
- Весь код проекта
- Каталог forum/ (XenForo)
- Дампы базы данных (если есть)
- Метка `Type: BASELINE` в `.backup-info`

**Создание:**
```bash
.git/hooks/backup-manager.sh create --baseline
```

**Особенность:** Baseline бэкапы **не удаляются** при ротации!

### Обычный бэкап
Инкрементальный снимок при Git-операциях:
- Весь код проекта
- Каталог forum/
- **Без** базы данных (для скорости)

### Бэкап с БД
Обычный бэкап + дампы базы данных:
```bash
.git/hooks/backup-manager.sh create --include-db
```

## Ротация бэкапов

**Правило:** хранятся бэкапы за последние **100 дней**.

- Старые бэкапы удаляются автоматически при создании нового
- **Baseline бэкапы сохраняются всегда**
- Удаление происходит только если новый бэкап создан успешно
- Проверка ротации: `.git/hooks/backup-manager.sh rotate`

## Восстановление из бэкапа

### Полное восстановление проекта

```bash
# 1. Найдите нужный бэкап
.git/hooks/backup-manager.sh list

# 2. Просмотрите информацию о бэкапе
cat ~/SinbadBackups/2025-12-05_19-35-35/.backup-info

# 3. Восстановите весь проект
rsync -av ~/SinbadBackups/2025-12-05_19-35-35/ ~/Desktop/sinbad-portal/
```

### Восстановление отдельных файлов

```bash
# Восстановить один файл
cp ~/SinbadBackups/2025-12-05_19-35-35/app/config.php ~/Desktop/sinbad-portal/app/

# Восстановить директорию
rsync -av ~/SinbadBackups/2025-12-05_19-35-35/forum/ ~/Desktop/sinbad-portal/forum/
```

### Восстановление базы данных

```bash
# Найти дампы БД в бэкапе
ls ~/SinbadBackups/2025-12-05_19-35-35/db_dump/

# Восстановить на сервер (если нужно)
scp ~/SinbadBackups/2025-12-05_19-35-35/db_dump/*.sql root@server:/tmp/
```

## Управление бэкапами

### Просмотр списка бэкапов

```bash
.git/hooks/backup-manager.sh list
```

Вывод:
```
📦 Available backups:
  2025-12-05_19-35-35 (109M) [BASELINE] [+DB]
  2025-12-05_18-24-15 (108M)
  2025-12-05_15-52-25 (108M)
  ...

Total backups: 7
Total size: 756M
```

### Просмотр лога операций

```bash
tail -f ~/SinbadBackups/backup.log
```

### Тестирование ротации

```bash
# Показать какие бэкапы будут удалены (без удаления)
.git/hooks/backup-manager.sh --test-rotation
```

### Принудительная ротация

```bash
.git/hooks/backup-manager.sh rotate
```

## Метаданные бэкапа

Каждый бэкап содержит файл `.backup-info`:

```
Backup created: 2025-12-05_19-35-35
Project: Sinbad Portal
Reason: manual
Git branch: main
Git commit: 81f3d5f
Include DB: true
Type: BASELINE
```

И файл `.backup-manifest` со списком всех файлов.

## Git-хуки

Система использует следующие Git-хуки:

| Хук | Событие | Причина в логе |
|-----|---------|----------------|
| `post-commit` | После commit | `commit` |
| `post-merge` | После merge/pull | `merge` |
| `post-checkout` | После checkout | `checkout` |
| `post-rewrite` | После amend/rebase | `rewrite-amend` / `rewrite-rebase` |
| `post-applypatch` | После git am | `applypatch` |

Все хуки находятся в `.git/hooks/` и вызывают `backup-manager.sh`.

## Troubleshooting

### Бэкап не создаётся автоматически

Проверьте права на выполнение:
```bash
ls -la .git/hooks/
chmod +x .git/hooks/post-*
chmod +x .git/hooks/backup-manager.sh
```

### Слишком много бэкапов

```bash
# Проверьте размер бэкапов
du -sh ~/SinbadBackups/*

# Уменьшите период хранения (в backup-manager.sh)
RETENTION_DAYS=30  # вместо 100

# Или удалите старые бэкапы вручную (кроме baseline)
rm -rf ~/SinbadBackups/2025-11-*
```

### Бэкапы создаются без изменений

Это не должно происходить в v2.0. Если происходит:
```bash
# Проверьте лог
tail -20 ~/SinbadBackups/backup.log

# Убедитесь что используется новая версия
head -5 .git/hooks/backup-manager.sh
# Должно быть: "Backup Manager v2.0"
```

### Восстановление после критической ошибки

```bash
# 1. Найдите последний рабочий бэкап (желательно BASELINE)
.git/hooks/backup-manager.sh list

# 2. Создайте временную копию текущего состояния
mv ~/Desktop/sinbad-portal ~/Desktop/sinbad-portal.broken

# 3. Восстановите из бэкапа
rsync -av ~/SinbadBackups/2025-12-05_19-35-35/ ~/Desktop/sinbad-portal/

# 4. Проверьте состояние Git
cd ~/Desktop/sinbad-portal
git status
```

## Рекомендации

1. **Создайте baseline бэкап** после крупных изменений
2. **Регулярно проверяйте бэкапы**: раз в неделю убедитесь, что бэкапы создаются
3. **Не храните бэкапы только локально**: периодически копируйте на внешний диск или в облако
4. **Тестируйте восстановление**: периодически проверяйте, что восстановление работает
5. **Следите за местом**: большие проекты могут занимать много места

## Интеграция с Antigravity

Antigravity автоматически использует эту систему:

- **Перед операцией**: выполняется синхронизация
- **После операции**: создаётся бэкап (если есть изменения)
- **Перед push**: проверяется чистота проекта

Это обеспечивает безопасность при автоматическом рефакторинге.

## Размер бэкапов

Типичные размеры:
- **Baseline бэкап**: ~109M (весь проект + forum + БД)
- **Обычный бэкап**: ~108M (весь проект + forum, без БД)
- **Инкрементальный прирост**: минимальный (rsync копирует только изменения)
