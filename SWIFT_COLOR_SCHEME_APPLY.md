# ИНСТРУКЦИЯ: Применение цветовой схемы Swift – Color scheme 2 к Swift – Sinbad

## ⚠️ ВАЖНО
Выполните эти шаги **в админ-панели XenForo** через веб-интерфейс.  
Все действия выполняются **только через Style properties**, без редактирования кода.

---

## Контекст

В теме Swift цветовые схемы реализованы как отдельные стили-доноры.  
Их нельзя использовать напрямую — значения нужно перенести в рабочий child-style.

**Исходные стили:**
- **Базовый макет:** Swift – gridOne
- **Рабочий стиль:** Swift – Sinbad
- **Донор цвета:** Swift – Color scheme 2

---

## Этап 1 — Фиксация значений из донора

1. Открыть в админ-панели XenForo: **Appearance → Styles**
2. Найти стиль: **Swift – Color scheme 2**
3. Открыть: **Swift – Color scheme 2 → Style properties**
4. Зафиксировать значения параметров в следующих разделах:

### 📋 Разделы для копирования:

#### 1. **Colors** (основные цвета интерфейса)
   - Primary color
   - Secondary color
   - Tertiary color
   - Accent color
   - Все остальные цветовые параметры в этом разделе

#### 2. **Backgrounds** (фон страницы, контейнеров, блоков)
   - Page background
   - Content background
   - Block background
   - Header background
   - Footer background
   - Все фоновые параметры

#### 3. **Text** (основной и вторичный текст)
   - Primary text color
   - Secondary text color
   - Muted text color
   - Все параметры текста

#### 4. **Links** (ссылки, hover)
   - Link color
   - Link hover color
   - Link active color
   - Все параметры ссылок

#### 5. **Borders / Dividers** (границы, разделители)
   - Border color
   - Border radius
   - Divider color
   - Все параметры границ и разделителей

**💡 Совет:** Запишите или скопируйте значения в текстовый редактор для удобства переноса.

---

## Этап 2 — Перенос значений в рабочий стиль

5. Открыть: **Swift – Sinbad → Style properties**
6. Перейти в соответствующие разделы (Colors, Backgrounds, Text, Links, Borders/Dividers)
7. Вручную перенести значения из **Swift – Color scheme 2**:

   - ✅ Значения должны совпадать **1:1**
   - ✅ Если параметра нет в Swift – Sinbad — пропустить
   - ✅ Не изменять параметры, которых нет в доноре
   - ✅ Сохранять структуру разделов

### Порядок действий:

1. Открыть раздел **Colors** в Swift – Sinbad
2. Сравнить с разделом **Colors** в Swift – Color scheme 2
3. Скопировать каждое значение вручную
4. Повторить для всех разделов: Backgrounds, Text, Links, Borders/Dividers
5. Проверить, что все значения перенесены корректно

---

## ⚠️ ЗАПРЕЩЕНО

- ❌ **Активировать** Swift – Color scheme 2
- ❌ **Менять родительский стиль** Swift – Sinbad
- ❌ **Редактировать шаблоны** (Templates)
- ❌ **Добавлять Custom CSS / LESS**
- ❌ **Устанавливать аддоны**
- ❌ **Использовать автоматический импорт** цветовой схемы

---

## Этап 3 — Сохранение изменений

8. После переноса всех значений:
   - Нажать **Save** в каждом разделе
   - Или нажать **Save** в конце (если доступно)
9. Убедиться, что изменения сохранены:
   - Проверить, что значения отображаются в Swift – Sinbad
   - Swift – Color scheme 2 должен остаться **неактивным**

---

## Результат

✅ Стиль **Swift – Sinbad** использует цветовую схему №2 поверх макета gridOne  
✅ Цветовой стиль **Swift – Color scheme 2** остаётся неактивным и используется только как донор  
✅ Архитектура XenForo не нарушена  
✅ Обновляемость темы сохранена  

---

## Проверка после применения

После выполнения всех этапов проверьте:

✅ **Swift – Sinbad** отображает цвета из Color scheme 2  
✅ **Swift – Color scheme 2** остаётся неактивным (не назначен по умолчанию)  
✅ Форум отображается с новой цветовой схемой  
✅ Все разделы Style properties содержат корректные значения  
✅ Нет ошибок в админ-панели  

---

## Откат изменений (если необходимо)

Если нужно вернуться к предыдущим цветам:

1. Перейти: **Swift – Sinbad → Style properties**
2. Вручную вернуть предыдущие значения
3. Или использовать значения из другого донора
4. Сохранить изменения
5. Очистить кэш XenForo

---

## Git

**⚠️ ВАЖНО:** Если файловая система не затронута — **git commit / git push НЕ выполнять**.

Изменения цветовой схемы хранятся в базе данных XenForo, а не в файлах проекта.  
Git-коммит нужен только если были изменены файлы в файловой системе.

---

## Примечания

- Цветовые схемы в Swift реализованы как отдельные стили для удобства переключения
- Перенос значений вручную гарантирует корректную работу child-style
- Все изменения сохраняются в базе данных XenForo
- Child-style наследует макет от родительского стиля, но переопределяет цвета
- После обновления темы Swift нужно будет проверить, не изменились ли параметры




